#!/system/bin/sh

echo "Content-Type: application/json"
echo

CACHE="/data/adb/pixelfirewall/apps.json"

if [ -f "$CACHE" ]; then
    cat "$CACHE"
else
    printf '{"status":"ERROR","error":"Apps cache not found"}\n'
fi
